# AI Articles Collection

每周 AI 文章整理与分享的静态网站，基于 Nextra 构建。

## 特性

- 📝 Markdown 文件自动编译成页面
- 📂 左侧目录导航
- 🔍 内置搜索功能
- 🎨 简洁美观的界面
- 📱 响应式设计
- 🚀 SEO 优化
- ☁️ 可部署到 Vercel

## 开始使用

### 安装依赖

```bash
npm install
# 或
yarn install
# 或
pnpm install
```

### 开发

```bash
npm run dev
```

打开 [http://localhost:3000](http://localhost:3000) 查看网站。

### 构建

```bash
npm run build
```

构建后的静态文件在 `out` 目录中。

## 添加文章

1. 在 `pages` 目录下创建或编辑 markdown 文件（`.md` 或 `.mdx`）
2. 使用 frontmatter 添加元数据：

```markdown
---
title: 文章标题
description: 文章描述
---

# 文章标题

文章内容...
```

3. 在对应的 `_meta.json` 文件中添加导航项（可选）

## 部署到 Vercel

### 方式一：通过 Vercel Dashboard

1. 将代码推送到 GitHub/GitLab/Bitbucket
2. 在 [Vercel](https://vercel.com) 导入项目
3. Vercel 会自动检测 Next.js 项目并部署

### 方式二：使用 Vercel CLI

```bash
npm i -g vercel
vercel
```

## 项目结构

```
.
├── pages/              # Markdown 文件目录
│   ├── _meta.json     # 根目录导航配置
│   ├── index.mdx      # 首页
│   └── 2024/          # 2024 年文章目录
│       ├── _meta.json # 子目录导航配置
│       └── *.mdx      # 文章文件
├── theme.config.tsx   # 主题配置
├── next.config.js     # Next.js 配置
└── package.json       # 依赖配置
```

## 许可证

MIT
